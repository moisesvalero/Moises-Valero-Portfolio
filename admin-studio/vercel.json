{
	"$schema": "https://openapi.vercel.sh/vercel.json",
	"installCommand": "cd .. && pnpm install --frozen-lockfile",
	"buildCommand": "cd .. && pnpm run build:studio",
	"outputDirectory": "../dist",
	"rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
